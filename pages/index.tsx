import type { NextPage } from 'next';
import Navbar from '../components/Layout/Navbar';

const Home: NextPage = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default Home;
